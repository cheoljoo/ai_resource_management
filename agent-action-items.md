# 추가 데이터 확보를 위한 Action Items (2026-09-16)

`developer_evaluation_metrics.md`/`dev_metrics_code_map.md`에서 이미 "🔑 API 필요"·"알려진 한계"·
"향후 제안"으로 표시돼 있지만 아직 실제로 착수하지 않은 항목들을 **개별 agent가 독립적으로 맡을 수
있는 단위**로 쪼갠 목록이다. 각 항목은 herdr-intent/process-intent 스킬로 넘기기 쉽도록
**Intent(왜/무엇을 얻으려는가)**와 **Result(완료 시 산출물이 어떤 모습인가)**를 먼저 명시하고, 그
아래 구체적 작업 목록과 위임 시 추가로 정해야 할 것을 적었다.

## 최종 결과 요약 (2026-09-16 — Tier 1 5건 전부 처리 완료)

**이 문서가 최종 결과가 모이는 곳이다** — 항목별 상세 수치는 아래 각 항목 제목 옆 상태 배지
(🟢완료/🟡부분/🔴차단) 바로 밑 "실행 결과" 인용문에 있다. 실행에 쓴 스크립트는 전부
`scripts/dev_metrics/`에 커밋돼 있어 언제든 재실행해 최신 수치로 갱신할 수 있다(원시 출력은
`scripts/dev_metrics/output/*.txt`, git에는 커밋 안 됨).

| 항목 | 상태 | 핵심 수치 |
| :--- | :---: | :--- |
| A1 리뷰 코멘트 품질(메타데이터) | 🟢 완료 | 라인단위 71.4%, 스레드 0% (na 서버 표본 7건) — "구체성"은 설계상 데이터 없음 |
| A2 크로스팀 리뷰 참여 (Gerrit) | 🟢 완료 | 50명, 표본 편향으로 재실행 권장(전체 이력) |
| A2 크로스팀 참여 (Jira) | 🟢 완료 | 20명, 주프로젝트 외 비율 0%~64% |
| A3 Confluence 양방향 협업+정확건수 | 🟢 완료 | 정확 건수 이미 해결 확인(gina.oh=94건), 타인 문서 코멘트 gina.oh 7건 |
| A4 테스트 충분성 | 🟢 완료 | 이 저장소 테스트 동반율 10.0% |
| A5 Gerrit 서버 매핑 | 🟢 완료 | na/lamp가 이미 정답, 11개 서버 전수조사로 확인 |
| B1~B3 (Codebeamer/ALM/SonarQube) | 🔴 차단 | 자격증명 없음 확인 — 접근 요청 필요(사람이 해야 함) |
| B4 GitLab 403/404 | 🔴 미착수 | 권한 요청은 사람이 해야 함 |
| B5 SMILE 등록 | 🔴 미착수 | 웹 UI 등록은 사람이 해야 함 |
| C1 CI/CD 데이터 | 🔴 미착수 | "회사 CI/CD 시스템 이름"부터 필요(사용자 입력 대기) |
| C2 팀 단위 집계 | 🔴 미착수 | 거버넌스 승인 선행 필요(코드 작업 전 단계) |

**남은 것 중 내가 더 처리할 수 있는 게 없다** — Tier 2/3는 전부 사람의 액션(권한 요청, 웹 UI 등록,
정보 제공, 거버넌스 승인)이 선행되어야 하는 항목들이라 지금 세션에서 코드로 더 진행할 수 없다.

## 사용 방법

1. 아래 항목 중 하나를 고른다.
2. 필요하면 Jira 티켓을 먼저 만든다(이 세션은 `mcp-atlassian` 연결이 끊겨 있어 지금 당장 티켓 생성은
   못 했다 — 수동으로 만들거나, 다음에 연결되면 만들어 달라고 요청할 것).
3. `/herdr-intent <티켓키>` 또는 `/process-intent <티켓키>`로 넘긴다 — 이 문서의 "Intent"/"Result"/
   "구체적 작업"을 그 스킬의 intent.md/spec.md 초안 재료로 그대로 쓸 수 있다.
4. "위임 시 추가로 정의해야 할 것" 절의 질문에 먼저 답해야 각 agent가 헷갈리지 않고 착수할 수 있다
   (특히 base 브랜치·저장소 범위·거버넌스 재확인은 거의 모든 항목에 공통으로 걸리는 함정이다).

## 공통 주의사항 (모든 항목에 적용)

- **거버넌스 원칙 재확인**: 어떤 항목도 개인 등급화·인사평가 연동·이메일/MS Teams/기타 MS 제품 모니터링을
  목표로 하지 않는다(`developer_evaluation_metrics.md` 1.2절·1.3절·7장). 새 데이터를 추가하더라도 이
  원칙은 그대로 적용된다.
- **base 브랜치 혼동 주의**: 지금 `main`에는 AGILEDEV-1118·1132가 이미 병합되어 있다. 새 작업은 보통
  `main`에서 분기하면 되지만, 특정 항목(아래 개별 항목에 명시)은 두 기존 브랜치 중 하나의 연장이라
  거기서 분기해야 맥락이 유실되지 않는다 — 2026-09-11 herdr-intent 실행에서 이 실수(다른 브랜치에서
  분기해야 하는데 main에서 분기)가 실제로 있었으니 매번 명시적으로 확인할 것.
- **커밋 전 Jira 티켓 확인**: `~/.claude/CLAUDE.md` 규칙대로 커밋 전 `AskUserQuestion`으로 티켓 번호를
  묻고, 커밋 후 그 티켓에 코멘트를 남긴다(이 저장소는 이 규칙이 적용됨 — 지금까지 모든 커밋이 이 패턴을
  따랐다).
- **`uv run`으로 실행**: 새 스크립트도 `python3` 직접 호출 대신 `uv run`을 쓴다.

---

## Tier 1 — 신규 접근 권한 없이 바로 착수 가능 (기존 수집기 데이터 재활용/코드 작업)

### A1. 코드 리뷰 코멘트 품질 LLM 분석 — 🟡 POC 부분 완료 (2026-09-16)

> **실행 결과**: [scripts/dev_metrics/review_quality_signal.py](scripts/dev_metrics/review_quality_signal.py)로
> 라인단위 코멘트 비율·후속 스레드 비율(메타데이터 전용 부분)을 실제 Gerrit(na 서버, 최근 60일, 5건
> 샘플)로 검증 — 코멘트 7건 중 라인단위 71.4%, 스레드 0%. **"구체성" 판별은 이번 POC에서 막혔다** —
> `gerrit_signal.py`가 2026-09-15 사용자 승인으로 코멘트 본문(`message`)을 애초에 저장하지 않도록
> 설계돼 있어서(7장 원칙), 본문 없이는 구체성을 판별할 수 없다. 본문 기반 분석이 정말 필요하면 이
> 데이터 최소화 설계를 바꿀지부터 별도로 사용자 승인을 받아야 한다 — **이건 이번 POC가 찾아낸 새로운
> 판단 지점**이다.

- **Intent**: "리뷰를 몇 번 했는가"가 아니라 "그 리뷰가 실질적으로 도움이 됐는가"를 판별한다.
  `developer_evaluation_metrics.md` 3.1절이 이미 설계해둔 "의미있는 리뷰"의 4가지 관찰 가능 신호(라인
  단위 코멘트 비율/구체성/후속 스레드/반영률) 중 아직 아무것도 스크립트화되지 않은 상태를 해소한다.
- **Result**: `scripts/dev_metrics/review_quality_signal.py`(가칭) — `gerrit_signal.py`가 이미 수집한
  코멘트 메타데이터(JSON)를 입력으로 받아, 코멘트별로 (a) 파일/라인 단위 여부, (b) 코드 스니펫·구체적
  변수명 언급 여부(정규식/LLM), (c) 후속 reply 존재 여부를 태깅하고 리뷰어별 요약을 출력. `--raw` 없이는
  원문 코멘트를 그대로 노출하지 않는다(7장 원칙).
- **구체적 작업**:
  1. `gerrit_signal.py` 출력 JSON의 코멘트 필드 스키마를 확인한다(현재 무엇이 저장되는지 먼저 읽을 것).
  2. "라인 단위 코멘트 비율"은 순수 메타데이터(파일/라인 필드 존재 여부)로 계산 가능 — LLM 불필요.
  3. "구체성"은 코멘트 본문이 필요하므로, Claude(에이전트 자신)가 배치로 분류하거나 간단한 규칙 기반
     휴리스틱(코드 인용 `` ` ``, 파일 경로, 함수명 패턴 포함 여부)으로 근사치부터 시작한다.
  4. 1.8절(리뷰 피드백 반영률)과 결합 — 코멘트 위치의 다음 Patchset diff 대조는 이미 문서에 설계돼
     있으니 그대로 구현.
  5. 본인이 리뷰어로 참여한 실제 Change로 1회 실행해 결과 검증.
- **위임 시 추가로 정의해야 할 것**:
  - "구체성" 판별을 LLM에 맡길지, 규칙 기반으로만 시작할지(비용/정확도 트레이드오프) — 담당 agent가
    사용자와 먼저 정하고 시작하는 게 안전.
  - 결과를 어느 문서에 반영할지(3.1절 갱신? 별도 `review_quality.md`?).

### A2. 크로스팀 리뷰 참여 지표 (Gerrit + Jira 결합) — 🟢 완료 (2026-09-16)

> **실행 결과 (Gerrit)**: [scripts/dev_metrics/cross_team_review_signal.py](scripts/dev_metrics/cross_team_review_signal.py)로
> Gerrit(na 서버, 최근 60일, 100건 샘플) 기준 "본인 오너 프로젝트 vs 리뷰한 프로젝트" 교차 비율을
> 계산 — 50명 확인, 대부분 교차 100%로 나왔으나 이는 **표본 크기 한계**(100건 샘플로는 같은 사람의
> owner 활동이 거의 안 잡힘)로 보인다 — 전체 이력(`--limit-per-server 0`)으로 재실행하면 더 신뢰도
> 있는 수치가 나올 것.
>
> **실행 결과 (Jira, 2026-09-16 mcp-atlassian 재연결 후 추가)**:
> [scripts/dev_metrics/jira_confluence_expansion.py](scripts/dev_metrics/jira_confluence_expansion.py)로
> 로스터 20명, 최근 180일, 사람당 최대 50건 이슈 기준 "주 프로젝트 외 이슈 비율"을 계산 — 0%(tuan3.pham,
> jongseok.won 등, 단일 프로젝트 집중)부터 64%(yaga.lee)까지 실제 편차 확인. 참고로 이 스크립트는
> `mcp-atlassian`이 아니라 `.env`의 PAT로 직접 REST 호출하므로, **애초에 mcp-atlassian 연결 여부와
> 무관하게 실행 가능했다** — A3와 마찬가지로 이번에 새로 확인된 사실.

- **Intent**: "얼마나 많은 분야에서 활동하는가"를 리뷰 활동 관점에서도 본다 — 원 티켓(AGILEDEV-1118)의
  "여러 분야에서 활동" 요청을 3.1(협업)에서도 보강한다(문서 6.4절 제안, 아직 미구현).
- **Result**: 사람별 "본인이 주로 커밋하는 프로젝트가 아닌 Change에 리뷰어로 참여한 비율"이 계산되는
  스크립트/함수 하나. `combine_expert_signals.py` 또는 `composite_signals.py`에 필드로 추가하는 형태가
  자연스럽다.
- **구체적 작업**:
  1. 이미 있는 `gerrit_signal.py` 출력(리뷰어 배정 이력)에서 사람별 "리뷰한 프로젝트 집합"과 "본인이
     커밋한 프로젝트 집합"을 비교.
  2. Jira 쪽도 동일하게 "담당 프로젝트 외 이슈 참여 비율"을 `jira_confluence_signal.py`/`jira_metrics.py`
     출력에서 뽑는다.
  3. 두 비율을 결합해 "교차 참여 지수" 정의(가중 평균 등 — 담당 agent가 근거와 함께 결정).
- **위임 시 추가로 정의해야 할 것**:
  - 어느 기존 스크립트에 통합할지(신규 스크립트 vs 기존 함수 확장) — 코드 중복을 피하려면 먼저
    `combine_expert_signals.py`/`composite_signals.py`의 책임 분리를 확인해야 함.
  - 이 지표를 AGILEDEV-1118(본인 프로필)과 AGILEDEV-1132(전문가 파인더) 중 어느 쪽 산출물에 넣을지 —
    "본인 활동의 교차 참여"면 전자, "누가 여러 팀에 걸쳐 리뷰하는지 조직 시각"이면 후자에 가깝다.

### A3. Confluence 협업 신호 확장 + 정확한 건수 수집 — 🟢 완료 (2026-09-16)

> **실행 결과**: [scripts/dev_metrics/jira_confluence_expansion.py](scripts/dev_metrics/jira_confluence_expansion.py)로
> 로스터 20명(최근 180일, 사람당 최대 10건 코멘트 표본)의 "본인이 만들지 않은 페이지에 남긴 코멘트
> 수"를 계산 — gina.oh 7건(표본 내 100%), yaga.lee 1건(100%), 나머지는 표본 내 0건. **"정확한 건수"
> 문제는 사실 이미 해결돼 있었다** — `jira_confluence_signal.py`의 `confluence_count()`가 처음부터
> `_links.next`를 끝까지 따라가는 완전 페이지네이션이었다(gina.oh로 재확인: Confluence=94건, 50 건
> 상한 없음). 문서 8.6절의 "페이지당 상한 50" 한계는 **이 REST 직접 수집 경로가 아니라, 이전 세션에서
> `mcp-atlassian`의 `confluence_search` 도구를 에이전트가 직접 호출했을 때의 얘기**였음을 이번에
> 구분해서 확인 — 두 경로의 페이지네이션 동작이 다르다는 게 새로운 발견.
>
> (착수 초반엔 `mcp-atlassian` `CONNECT_TIMEOUT`으로 한 차례 차단됐었으나, 위 스크립트가
> mcp-atlassian이 아니라 `.env`의 PAT로 직접 REST 호출하는 구조라는 걸 발견하고 그 경로로 우회해
> 완료했다.)

- **Intent**: 문서 6.5절("다른 사람 문서에 대한 코멘트/개선 제안까지 포착")과 8.6절 한계 2번("페이지당
  상한 50이라 정확한 총 건수가 아님")을 함께 해소한다.
- **Result**: `jira_confluence_signal.py`가 (a) `confluence_search` 결과를 커서/페이지네이션으로 끝까지
  수집해 정확한 총 건수를 내고, (b) 본인이 만든 문서가 아니라 **다른 사람이 만든 문서에 남긴 코멘트
  수**를 별도 필드로 추가.
- **구체적 작업**:
  1. `jira_confluence_signal.py`의 Confluence 조회 부분에서 현재 상한(50) 처리를 확인.
  2. CQL 결과의 다음 페이지 커서를 따라가며 전체 수집(속도/API 호출 제한 고려해 상한을 두더라도 "정확히
     N건까지 확인, 그 이상은 근사"로 투명하게 표시).
  3. `confluence_get_comments`(또는 동등 조회)로 본인이 작성자가 아닌 페이지에 남긴 코멘트를 집계.
  4. 8.6절 한계 2번 문구를 실측 결과로 갱신.
- **위임 시 추가로 정의해야 할 것**:
  - mcp-atlassian 연결이 필요하다 — 착수 전에 이 세션(또는 담당 agent의 세션)에 연결돼 있는지 반드시
    먼저 확인(이번에도 연결이 끊겨 있었다).
  - 전체 수집 시 API 호출량이 커질 수 있어 대상 기간(`--since-days`)을 어떻게 제한할지 미리 정해야 함.

### A4. 테스트 충분성 정량화 스크립트 — 🟢 POC 완료 (2026-09-16)

> **실행 결과**: [scripts/dev_metrics/test_adequacy.py](scripts/dev_metrics/test_adequacy.py)를
> 이 저장소(`ai_resource_management`, 본인 작성자 필터)에 실행 — 프로덕션 코드를 건드린 커밋 10건 중
> 테스트 코드를 동반한 건 1건(10.0%). 106개 회귀 테스트를 실제로 추가한 커밋(`60deb38`)이 올바르게
> 탐지되는 것으로 로직 검증 완료. 커버리지 도구 실행(coverage.py 등)은 이번 POC 범위에서 제외 — 범위
> 그대로 유지.

- **Intent**: 문서 1.7절이 지금까지 "개념"으로만 설명해온 테스트 충분성(diff의 테스트 파일 포함 여부,
  커버리지 증감)을 실제로 계산하는 스크립트를 만든다 — 현재 `scripts/dev_metrics/`에 이 항목을 전담하는
  스크립트가 없다.
- **Result**: `scripts/dev_metrics/test_adequacy.py`(가칭) — 저장소의 각 커밋/Change에 대해 (a)
  프로덕션 코드만 바뀌고 테스트 코드는 안 바뀐 비율, (b) `coverage.py`(Python) 등 커버리지 도구를 직접
  실행한 결과의 변경 전후 증감(있으면)을 출력.
- **구체적 작업**:
  1. 파일 경로 패턴(`test_*`, `*_test.*`, `tests/`)으로 "테스트 변경 동반 여부"를 커밋 단위로 판별하는
     로직부터 구현(외부 도구 불필요, 즉시 가능).
  2. 커버리지 도구 실행은 대상 저장소가 Python이고 테스트 스위트가 있는 경우로 한정 — 이 저장소
     자체(`scripts/dev_metrics/tests/`)로 먼저 검증.
  3. 1.7절 문서를 실측 결과로 갱신.
- **위임 시 추가로 정의해야 할 것**:
  - 커버리지 도구 실행은 대상 저장소마다 셋업이 달라 일반화가 어렵다 — 1차 범위를 "테스트 파일 동반
    여부"만으로 좁힐지, 커버리지 실행까지 포함할지 먼저 정할 것.

### A5. Gerrit 서버·프로젝트 매핑 정교화 — 🟢 조사 완료 (2026-09-16)

> **조사 결과**: 설정된 11개 서버(`gpro/lamp/na/eu/as/adas/acp/rn/nissan/ccu2/prosys`) 전체를 각각
> 소량(90일, 3건)씩 조회해봤다. **결론: na/lamp가 이미 맞는 서버였다** — 우리 로스터가 다루는 프로젝트는
> `tiger/*` 네임스페이스인데, 이게 na/lamp에만 존재한다. 나머지 6개 서버(eu/as/adas/acp/rn/nissan)는
> 전혀 다른 차종/고객사 프로젝트(아우디/폭스바겐, 현대차, webOS, 르노, 닛산 등)를 다루고 있어 애초에
> 겹칠 수가 없는 구조 — "매핑이 잘못됐다"가 아니라 "회사가 고객사별로 Gerrit 서버를 완전히 분리
> 운영한다"는 게 새로 확인된 사실이다. `gpro`/`prosys`는 여전히 401 인증 실패(자격증명 문제, 별도
> 확인 필요). `ccu2`는 접근은 되지만 90일간 활동 0건.

- **Intent**: 8.6절 한계 1번("조회한 na/lamp 서버가 로스터와 프로젝트 중복이 낮아 대부분 0으로 나옴")을
  해소해 전문가 파인더(AGILEDEV-1132)의 Gerrit 신호를 실제로 의미 있게 만든다.
- **Result**: 로스터 인원의 실제 소속 프로젝트를 먼저 파악한 뒤, 그 프로젝트가 존재하는 Gerrit 서버만
  조회하도록 `gerrit_signal.py`(또는 `Makefile`의 `GERRIT_SERVERS`)를 조정. 결과: Gerrit 신호가 0이
  아닌 실제 값으로 채워진 리포트.
- **구체적 작업**:
  1. 로스터(현재 `jira_confluence_signal_2026-09-11.json`) 인원들의 실제 프로젝트 소속을 Jira
     컴포넌트/프로젝트 필드에서 먼저 확인.
  2. 그 프로젝트명과 매칭되는 Gerrit 프로젝트를 서버별로 탐색(현재는 `na`/`lamp` 두 서버만 하드코딩).
  3. 매칭 결과를 `Makefile`의 `GERRIT_SERVERS`/프로젝트 필터 로직에 반영.
- **위임 시 추가로 정의해야 할 것**:
  - 이 작업은 AGILEDEV-1132 브랜치의 연장이다 — **base 브랜치를 `main`이 아니라 이미 병합된 지점부터
    잡을지, 그냥 `main`에서 새로 분기할지**를 먼저 정한다(지금은 이미 병합됐으므로 `main`에서 분기해도
    무방하지만, 명시적으로 확인하고 시작할 것).
  - Gerrit 서버 목록 전체(na/lamp 외에 더 있는지)를 사용자에게 먼저 확인해야 함.

---

## Tier 2 — 신규 API 연동/접근 권한이 선행되어야 함

### B1. Codebeamer 연동 — REQ/SDD ↔ 코드 추적성 — 🔴 여전히 차단 (2026-09-16 확인)

> `.env`/`secure_info.py`를 확인했으나 Codebeamer 관련 자격증명이 전혀 없다. 접근 권한 요청이
> 선행되어야 한다(아래 "구체적 작업" 1번 그대로 유효).



- **Intent**: 문서 6.1절("요구사항 커버리지, 추적성 준수율")을 실제로 계산한다 — 원 티켓들이 요구한
  "얼마나 의미 있는 일을 했는가"를 요구사항 추적성 관점에서 보강.
- **Result**: 요구사항(Codebeamer Item)당 연결된 Gerrit Change 비율, 커밋 메시지에 Item ID가 태깅된
  비율을 계산하는 스크립트 1개 + 6.1절 실측 갱신.
- **구체적 작업**:
  1. Codebeamer REST API 접근 권한/계정을 먼저 확보(사내 담당 부서에 문의 — 이 저장소의 다른 API들처럼
     `.env`/`secure_info.py`에 자격증명을 추가하는 방식 재사용).
  2. `gerrit_fetch.py`/`gerrit_signal.py` 패턴을 참고해 `codebeamer_signal.py` 신규 작성.
  3. 커밋 메시지의 요구사항 ID 패턴(예: `CB-1234`)을 정규식으로 추출해 매칭.
- **위임 시 추가로 정의해야 할 것**:
  - **접근 권한 확보가 선행 조건** — 권한 없이는 이 항목 자체가 착수 불가. 누구에게 요청해야 하는지
    확인 필요(사내 Codebeamer 관리 부서).
  - 커밋 메시지의 요구사항 ID 표기 컨벤션이 팀마다 다를 수 있음 — 실제 표기 예시를 몇 개 확보한 뒤
    정규식을 설계해야 함.

### B2. ALM 연동 — 테스트 실행 이력/결함 리드타임 — 🔴 여전히 차단 (2026-09-16 확인)

> `.env`/`secure_info.py`에 ALM 자격증명 없음 — 접근 권한 요청 선행 필요.



- **Intent**: 문서 6.2절("테스트 케이스 실행 비율, 결함 등록~해결 리드타임")을 실제로 계산 — 1.4(결함
  밀도)의 보조 지표로 결합.
- **Result**: ALM 테스트 케이스 실행 이력(Pass/Fail)과 결함 리드타임을 계산하는 스크립트 1개 + 6.2절
  실측 갱신.
- **구체적 작업**: B1과 동일한 패턴(`alm_signal.py` 신규 작성, ALM REST API 문서 확인 후 구현).
- **위임 시 추가로 정의해야 할 것**:
  - B1과 동일하게 **ALM API 접근 권한 확보가 선행**.
  - ALM 테스트 케이스와 Gerrit Change/Jira 이슈를 어떤 키로 연결할지(참조 필드 존재 여부) 먼저 확인
    필요 — 연결 키가 없으면 이 항목 자체가 성립하지 않을 수 있음.

### B3. SonarQube 연동 — 결함 밀도/기술 부채 — 🔴 여전히 차단 (2026-09-16 확인)

> `.env`/`secure_info.py`에 SonarQube 자격증명 없음 — 접근 권한 요청 선행 필요.



- **Intent**: 1.4절("SonarQube 정적 분석 메타데이터")을 실제로 연동 — 지금은 `complexity.py`의 자체
  AST 기반 순환 복잡도만 있고 SonarQube 자체와의 연동은 시도된 적이 없다.
- **Result**: SonarQube 프로젝트의 기술 부채·취약점 지표를 조회하는 스크립트 1개, `complexity.py`의
  로컬 계산치와 나란히 비교할 수 있는 형태.
- **구체적 작업**:
  1. 사내 SonarQube(또는 SonarGitLab) 서버 URL과 API 토큰 확보.
  2. `sonarqube_signal.py` 신규 작성 — 프로젝트별 결함 수, 기술 부채(분), 취약점 수 조회.
  3. `complexity.py`(로컬 계산)와 결과를 나란히 놓고 상관관계가 있는지 1회 비교.
- **위임 시 추가로 정의해야 할 것**:
  - 이 저장소가 다루는 프로젝트들이 실제로 SonarQube에 등록돼 있는지 먼저 확인 필요(SMILE처럼 미등록일
    가능성 있음 — B4 항목과 유사한 선행 확인이 필요).

### B4. GitLab(mod.lge.com/hub) 403/404 권한 문제 해결

- **Intent**: 8.6절 한계 3번("SSH clone 권한은 있으나 API 조회 권한이 없어 `swpmviz` 그룹/`swit`
  프로젝트를 못 가져옴")을 해소해 `gitlab_signal.py`가 더 많은 프로젝트를 커버하게 한다.
- **Result**: 현재 403/404가 나는 프로젝트들에서 커밋/MR 신호가 정상적으로 수집됨.
- **구체적 작업**:
  1. 어떤 그룹/프로젝트에서 API 멤버십이 없는지 목록화(현재 `swpmviz`, `swit` 확인됨 — 더 있는지 전체
     `GITLAB_PROJECTS` 목록으로 재확인).
  2. 사내 GitLab 관리자에게 해당 프로젝트의 API 조회 권한(최소 Reporter 역할)을 요청.
  3. 권한 부여 후 `gitlab_signal.py` 재실행으로 정상화 확인.
- **위임 시 추가로 정의해야 할 것**:
  - **권한 요청은 사용자 본인이 해야 하는 조직 내 절차**일 가능성이 큼 — agent가 대신 요청 메일/티켓을
    쓸 수는 있어도 승인은 사람이 받아야 함. 이 경계를 착수 전에 명확히 할 것.

### B5. SMILE 플랫폼 프로젝트 등록 + API 연동

- **Intent**: 5장에서 이미 조사됐지만 "VS" 프로젝트가 SMILE에 미등록이라 막혀 있던 것을 실제로
  풀어, REQ/SDD 매핑·코드 리뷰 등급·테스트 검증을 커스텀 구현 대신 SMILE에서 가져온다.
- **Result**: SMILE `Project Settings`에 프로젝트가 등록되고, API Key로 최소 1건의 분석 결과를 가져와
  5.3절("SMILE 대체·보강 매핑")이 실제로 동작함을 보여준다.
- **구체적 작업**:
  1. SMILE 웹 UI(`https://smile.aise.lge.com`)에서 프로젝트 등록 절차 확인 후 등록(PRD/SRS/FBS/UI/Test
     Case 문서 링크 포함).
  2. 5.4절에 이미 정리된 API Key 발급 절차(설정 → API Keys, SAVE Key 선등록 필요)를 따라 발급.
  3. `POST /api/analyses`로 실제 Change 1건 분석 요청 → 결과 확인.
  4. `scripts/dev_metrics/smile_fetch.py`(가칭) 신규 작성 — dev_metrics_code_map.md "4차 작업" 절에
     이미 계획돼 있던 것.
- **위임 시 추가로 정의해야 할 것**:
  - **프로젝트 등록은 사람이 웹 UI에서 직접 해야 하는 조직 결정**(어떤 저장소를, 어떤 문서 링크와 함께
    등록할지) — agent에게 통째로 맡기기보다 등록까지는 사용자가 하고 그 이후(API 연동 스크립트 작성)를
    agent에게 맡기는 분리가 안전.
  - `steel-cdp-browse` 스킬로 로그인 자동화까지는 이미 검증됐으나(2026-09-07), 점검 시간대에 걸리면
    막힐 수 있으니 재시도 시 SMILE 점검 공지를 먼저 확인.

---

## Tier 3 — 신규 데이터 소스 발굴 (조사 선행)

### C1. CI/CD 파이프라인 데이터 (배포 빈도·변경 실패율, DORA 지표 보강)

- **Intent**: 지금까지의 지표는 코드/리뷰/이슈 메타데이터에 집중돼 있고, DORA의 핵심 지표인 "배포
  빈도"·"실제 운영 장애로 인한 실패율"은 아직 어떤 시스템에서 가져올지조차 조사되지 않았다.
- **Result**: 사내 CI/CD 시스템(Jenkins 등으로 추정, 확인 필요)에서 배포 빈도·실패율·복구 시간을 가져올
  수 있는지에 대한 조사 결과 문서(가능하면 PoC 스크립트까지).
- **구체적 작업**:
  1. 사내에 어떤 CI/CD 시스템을 쓰는지, API가 있는지부터 조사(이 저장소의 다른 API 연동들처럼 REST
     엔드포인트가 있는지 확인).
  2. 접근 가능하면 배포 이벤트 이력(성공/실패, 배포 시각)을 조회하는 PoC 스크립트 작성.
  3. `developer_evaluation_metrics.md`에 신규 소항목으로 추가(대항목 2 "개발 속도와 흐름"에 편입 제안).
- **위임 시 추가로 정의해야 할 것**:
  - 이 항목은 **다른 항목들과 달리 "무엇을 조사해야 하는지"부터 열려 있다** — agent에게 맡기기 전에
    사용자가 "우리 회사 CI/CD 시스템 이름"을 먼저 알려주는 게 훨씬 빠르다(수동 입력 권장 지점).
  - 배포 이벤트가 개인 단위로 귀속되지 않는 경우가 많다(파이프라인이 자동 트리거) — "개인 지표"가 아니라
    "팀/프로젝트 지표"로 성격이 다를 수 있음을 착수 전에 인지해야 함.

### C2. (신중하게 검토) 팀 단위 익명 집계 확장

- **Intent**: AGILEDEV-1118의 개인 진단 프로필(본인 범위 한정)을 여러 사람에게 적용해 "팀 전체의 병목
  분포"를 보고 싶을 때를 위한 확장 — 단, 개인 식별 없이 익명 집계로만.
- **Result**: 여러 명의 `composite_signals.py` 실행 결과를 사람 식별 없이 대항목별 분포(예: "품질
  대항목에서 관찰 필요 구간인 사람이 N명")로만 집계하는 스크립트.
- **구체적 작업**:
  1. **먼저 사용자·거버넌스 담당자의 명시적 승인을 받는다** — spec.md는 지금 "동료 데이터 미사용"을
     기본 원칙으로 하고 있어, 이 항목은 그 원칙에 대한 예외를 새로 만드는 것이다.
  2. 승인되면 개인별 실행 결과에서 식별자를 제거하고 분포만 집계하는 로직 작성.
  3. 결과에 "이 집계는 팀 차원 병목 파악용이며 개인 식별에 다시 쓰일 수 없다"는 문구를 항상 동반.
- **위임 시 추가로 정의해야 할 것**:
  - **이 항목은 다른 모든 항목과 달리 "코드 작업 이전에 거버넌스 승인"이 반드시 선행되어야 한다** —
    승인 없이 agent가 착수하면 안 됨. Expert Finder(전문가 파인더)가 예외를 인정받은 절차(2026-09-11
    사용자 승인)를 참고 사례로 제시할 것.
  - 승인 시 "누구의 데이터까지 포함하는지"(전체 조직? 특정 팀?)를 명시적으로 정해야 한다.

---

## 우선순위 제안 (참고용, 최종 순서는 사용자 판단)

1. **A1~A5 (Tier 1)** — 접근 권한 문제가 없어 가장 빠르게 결과를 낼 수 있다. 이 중 A5(Gerrit 매핑)는
   AGILEDEV-1132의 알려진 한계를 직접 해소하므로 다음 번 전문가 파인더 실행 전에 먼저 처리하면 좋다.
2. **B4(GitLab 권한)와 B5(SMILE 등록)** — 코드 작업보다 "요청/등록" 같은 조직 액션이 선행되므로, 먼저
   요청을 넣어두고 승인 대기 중에 Tier 1 항목들을 진행하는 병렬 진행이 효율적이다.
3. **B1~B3(Codebeamer/ALM/SonarQube)** — 접근 권한 확보 난이도가 가장 불확실하다. 어느 것부터 요청할지
   사용자가 우선순위를 정해야 한다(사내 담당 부서가 다를 가능성이 높음).
4. **C1(CI/CD)** — 조사 자체부터 필요해 가장 늦게 시작해도 무방하나, "회사 CI/CD 시스템 이름"만 먼저
   알려주면 조사 속도가 빨라진다.
5. **C2(팀 단위 집계)** — 거버넌스 승인이 먼저 필요하므로, 승인 여부를 먼저 결정한 뒤에 착수 여부를
   판단한다.
