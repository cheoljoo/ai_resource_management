# Plan — AGILEDEV-1132

**상태**: 아직 착수 전.

## 착수 전 확인

- [ ] **베이스 브랜치 재확인** — 이 intent는 `main`이 아니라 `intent/2026-09-09-developer-expertise-grading`
      브랜치(커밋 `231c41f`)에서 분기했다(herdr-intent 실행 중 판단, 사용자 미확인 상태). 착수 전에
      사용자에게 "이 브랜치를 베이스로 쓰는 게 맞는지" 재확인한다 — 만약 아니라면(예: `main` 기준으로
      새로 시작하고 싶다면) worktree를 다시 만들어야 한다.
- [ ] `spec.md`의 후보 접근 A/B/C 중 어디로 갈지, 그리고 `intent.md` "풀어야 할 것" 4번(AGILEDEV-1118의
      Recognition/Warning Signal과의 관계)을 사용자와 먼저 정렬한다.
- [ ] `scripts/dev_metrics/expert_finder.py`, `experience_atoms.py`, `monthly_activity_clusters.py`를
      먼저 실행해보고 현재 동작/출력 포맷을 정확히 파악한다(문서만 보고 짐작하지 않는다).
- [ ] 다중 기여자 검증에 쓸 두 번째 저장소 후보를 정한다(`~/code` 하위에서 `git shortlog -sn --all`로
      기여자 3명 이상인 저장소를 탐색하는 방법을 우선 고려).
- [ ] 이 worktree에 `.env`가 심볼릭 링크로 연결되어 있는지 확인(`ls -la .env`) — Gerrit/Jira 데이터
      결합(spec.md B안)을 검토한다면 필요.

## 단계별 체크리스트

### 1. 검증
- [ ] `expert_finder.py --help`로 현재 옵션(저장소, 기간 등)을 확인한다.
- [ ] 기존 `pvs_crawler` 검증 결과를 재실행해 재현 가능한지 확인한다(회귀 기준선으로 삼는다).

### 2. 구현
- [ ] 14일 제한을 옵션화(또는 해제) — `spec.md` 완료조건 2번.
- [ ] 두 번째 이상의 저장소로 검증 확대 — 완료조건 3번.
- [ ] (B안을 택했다면) Gerrit/Jira 신호를 전문가 판정에 결합하는 로직 추가.
- [ ] (C안을 택했다면) 독립 문서/스크립트 세트로 재구성하고, 기존 6.6절과의 관계(참조/이관/중복 제거)를
      명확히 한다.
- [ ] 정성적 평가가 섞여 있지 않은지 최종 점검 — 완료조건 1번.

### 3. 검증 실행
- [ ] 최소 2개 저장소에서 실제 실행 결과를 만들고, "회사 안의 어떤 사람들이 전문가인지"에 답하는
      출력 예시를 확보한다 — 완료조건 4번.

### 4. 문서화
- [ ] `developer_evaluation_metrics.md` 6.6절 갱신 — 완료조건 5번.
- [ ] `dev_metrics_code_map.md` 매핑 표 갱신 — 완료조건 6번.

### 5. Jira 갱신
- [ ] AGILEDEV-1132에 진행 요약 댓글 게시(사용자 확인 후) — 완료조건 7번.
- [ ] 필요하면 AGILEDEV-1118과의 관계(이 clone 티켓이 그 티켓의 어느 부분을 대체/보완하는지)도 댓글에
      함께 남긴다.

이 저장소는 `/wiki-log`로 llm_wiki 중앙 저장소에 세션 로그를 남기는 관례를 쓴다 — 작업 완료 후
`/wiki-log` 실행을 잊지 않는다.
