# Plan — AGILEDEV-1118

**상태**: 아직 착수 전.

## 착수 전 확인

- [ ] `developer_evaluation_metrics.md`/`dev_metrics_code_map.md`의 현재 미커밋 변경분(390줄 규모)을
      먼저 검토하고, 필요하면 별도 커밋으로 정리한다 — 이 intent의 작업과 섞이지 않도록 구분해서
      커밋한다(같은 파일을 계속 수정하게 되므로).
- [ ] `spec.md`의 "후보 접근" A/B/C 중 어느 쪽으로 갈지 사용자와 먼저 정렬한다 — "등급"이라는 표현을
      그대로 쓸지, 리프레이밍할지가 이후 모든 산출물의 톤을 결정하므로 구현 전에 확정하는 게 좋다.
- [ ] "활동 폭/다양성" 지표의 데이터 소스(로컬 git 다중 저장소 / Gerrit 프로젝트 분포 / Jira 이슈 유형)
      중 무엇을 1차로 쓸지 확인 — Gerrit·Jira는 `.env` 자격증명과 `scripts/dev_metrics/gerrit_fetch.py`,
      `jira_metrics.py`가 이미 있어 재사용 가능하다는 점을 먼저 검토.
- [ ] 이 worktree에 `.env`가 심볼릭 링크로 연결되어 있는지 확인(`ls -la .env`) — Gerrit/Jira 실 데이터를
      쓰려면 필요.

## 단계별 체크리스트

### 1. 검증
- [ ] `scripts/dev_metrics/` 기존 스크립트들을 한 번씩 실행해보고 현재 출력 포맷(JSON/텍스트)을 파악한다
      — 새 로직이 이 출력들을 입력으로 재사용할 것이므로.
- [ ] `self_performance_report_2026-08.md`를 다시 읽고, 거기 나온 원시 수치(예: "31개 프로젝트 분산")를
      정식 지표로 승격할 때 재현 가능한지 확인한다.

### 2. 구현
- [ ] `developer_evaluation_metrics.md`에 "활동 폭/다양성" 소항목 추가(대항목 위치는 4장 "문제 정의 및
      설계 역량" 또는 신설 대항목 검토 — 기존 5개 대항목 중 어디에도 딱 맞지 않으면 담당 agent가 판단).
- [ ] 해당 지표 계산 스크립트를 `scripts/dev_metrics/`에 추가(기존 스크립트들의 CLI 인터페이스 관례를
      따른다 — `--repo`, `--author` 옵션 등).
- [ ] 기존 지표 스크립트 출력들을 종합하는 로직 구현(spec.md 완료조건 3번) — 새 스크립트 또는 기존
      스크립트를 오케스트레이션하는 상위 스크립트.
- [ ] Goodhart's Law/인사평가 금지 경고를 산출물에 반드시 포함시킨다(완료조건 5번).

### 3. 검증 실행
- [ ] 본인(cheoljoo.lee) 데이터로 종합 로직을 1회 실행해 결과 예시를 만든다.
- [ ] 결과가 `developer_evaluation_metrics.md`/`dev_metrics_code_map.md`에 정의된 지표 정의와 실제
      수치가 일치하는지(계산 로직 검산) 확인한다.

### 4. 문서화
- [ ] `dev_metrics_code_map.md`의 코드↔문서 매핑 표에 새 스크립트를 추가한다.
- [ ] 결과 예시를 `self_performance_report_2026-08.md`에 이어붙이거나 별도 파일로 남긴다(어느 쪽이
      나을지는 기존 파일의 "용도 제한" 문구와 일관되게 판단).

### 5. Jira 갱신
- [ ] 작업 완료 후 AGILEDEV-1118에 진행 요약 댓글을 남긴다(사용자 확인 필요 — 이 저장소가
      `~/.claude/CLAUDE.md`의 자동 댓글 규칙 대상인지 재확인. 대상이면 그 규칙을 그대로 따르고, 아니면
      사용자에게 직접 물어본다).
- [ ] 필요하면 이슈 상태를 갱신한다(`In Progress` → 다음 상태, 사용자 확인 후).

이 저장소에는 `test.md`/`plan.md`(루트)/`log.md` 같은 별도 결정 로그 관례가 아직 없다 — 대신
`/wiki-log` 스킬로 llm_wiki 중앙 저장소에 세션 로그를 남기는 관례가 있으므로, 작업 완료 후 `/wiki-log`
실행을 잊지 않는다.
